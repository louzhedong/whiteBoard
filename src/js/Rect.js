/**
 * 矩形类
 * @param {*} x 
 * @param {*} y 
 * @param {*} width 
 * @param {*} height 
 */
function Rect(x, y, width, height) {
  this.type = 'RECT';
  this.x = x;
  this.y = y;
  this.width = width;
  this.height = height;
}